; Use this file to override specific defaults in the stock Alpine php.ini
; NOTE: this only applies to web requests through the web server, not direct PHP instantiations like a background or cron task

memory_limit = 192M
max_execution_time = 60
request_terminate_timeout=60
